{
	frankenphp {
		# Laravel Octane-style workers (optional — enable for production-like perf)
		# num_threads 2
	}
}

http://{$SERVER_NAME:localhost} {
	root /app/public
	encode zstd br gzip

	php_server {
		try_files {path} index.php
	}

	log {
		output stdout
		format console
	}
}
